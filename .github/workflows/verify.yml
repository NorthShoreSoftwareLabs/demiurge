name: Verify

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: verify-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  node-floor:
    name: Node 22.13 package contract
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup minimum Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Redis
        run: sudo apt-get update && sudo apt-get install -y redis-server

      - name: Verify minimum-Node package contract
        run: pnpm --filter @demiurge-js/core test && pnpm test:pack

  verify:
    name: pnpm verify
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Install Redis
        run: sudo apt-get update && sudo apt-get install -y redis-server

      - name: Run verification
        run: pnpm verify

      # The text reporter already prints the per-file table into the log, and
      # per-file thresholds name the file that dipped. What is missing on a red
      # run is which lines, so upload the report then and not on the green runs
      # nobody opens.
      - name: Upload coverage report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: packages/core/coverage/
          retention-days: 7
          if-no-files-found: ignore
