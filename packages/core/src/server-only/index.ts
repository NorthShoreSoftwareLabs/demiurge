// A server environment resolves the default condition of the
// "@demiurgejs/core/server-only" package export to this file. The server can
// evaluate the module that declares it, so this file does nothing. Only the
// "browser" condition of the same package export enforces the boundary.
export {};
